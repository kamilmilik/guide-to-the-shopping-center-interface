<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kamilmilik.przewodnikpogaleriihandlowej">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainView.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainView.login.LoginActivity"
            android:label="Zaloguj się"
            android:parentActivityName=".MainView.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainView.accesandcontact.AccessAndContactActivity"
            android:label="Informacje"
            android:parentActivityName=".MainView.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainView.shops.ShopsActivity"
            android:label="Sklepy"
            android:parentActivityName=".MainView.MainActivity"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".MainView.shops.SelectedShopActivity"
            android:label="Sklep"
            android:parentActivityName=".MainView.shops.ShopsActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".MainView.parking.ParkingActivity"
            android:label="Parking"
            android:parentActivityName=".MainView.MainActivity"
            android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>